query hub($id: UUID_NAMEID!) {
  ecoverse(ID: $id) {
    id
    nameID
    displayName
    community {
      id
    }
    context {
      id
      references {
        id
        name
        description
        uri
      }
      visual {
        avatar
        background
        banner
      }
    }
  }
}
