query hub($id: UUID_NAMEID!) {
  hub(ID: $id) {
    id
    nameID
    displayName
    community {
      id
    }
    context {
      id
      references {
        id
        name
        description
        uri
      }
      visuals {
        name
        uri
      }
    }
  }
}
