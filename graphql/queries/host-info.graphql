query hostInfo($hubID: UUID_NAMEID!) {
  hub(ID: $hubID) {
    host {
      id
      nameID
      displayName
      profile {
        id
        tagsets {
          id
         name
         tags
        }
      }
    }
  }
}
