query hostInfo($hubID: UUID_NAMEID!) {
  hub(ID: $hubID) {
    host {
      id
      nameID
      profile {
        id
        displayName
        tagsets {
          id
         name
         tags
        }
      }
    }
  }
}
