query hostInfo($hubID: UUID_NAMEID!) {
  ecoverse(ID: $hubID) {
    host {
      id
      nameID
      displayName
      profile {
        id
        tagsets {
          id
         name
         tags
        }
      }
    }
  }
}
