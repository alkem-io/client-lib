query hostInfo($spaceID: UUID!) {
  lookup {
    space(ID: $spaceID) {
      account {
        id
        host {
          id
          nameID
          profile {
            id
            displayName
            tagsets {
              id
              name
              tags
            }
          }
        }
      }
    }
  }
}
