query hostInfo($ecoverseID: UUID_NAMEID!) {
  ecoverse(ID: $ecoverseID) {
    host {
      id
      nameID
      displayName
      profile {
        id
        tagsets {
          id
         name
         tags
        }
      }
    }
  }
}
