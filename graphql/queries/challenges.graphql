query challenges($hubID: UUID_NAMEID!) {
  hub(ID: $hubID) {
    challenges {
      id
      nameID
      profile {
        displayName
        visuals {
          name
          id
        }
      }
      community {
        id
        displayName
      }
      collaboration {
        id
      }
    }
  }
}
