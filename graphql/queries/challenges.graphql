query challenges($spaceID: UUID_NAMEID!) {
  space(ID: $spaceID) {
    challenges {
      id
      nameID
      profile {
        displayName
        visuals {
          name
          id
        }
      }
      community {
        id
        displayName
      }
      collaboration {
        id
      }
    }
  }
}
