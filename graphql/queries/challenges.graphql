query challenges($hubID: UUID_NAMEID!) {
  hub(ID: $hubID) {
    challenges {
      id
      nameID
      displayName
      context {
        visuals {
          name
          id
        }
    }
    }
  }
}
