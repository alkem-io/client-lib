query challenges($hubID: UUID_NAMEID!) {
  ecoverse(ID: $hubID) {
    challenges {
      id
      nameID
      displayName
      context {
        visuals {
          name
          id
        }
    }
    }
  }
}
