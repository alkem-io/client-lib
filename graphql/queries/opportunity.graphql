query opportunity($hubID: UUID_NAMEID!, $opportunityID: UUID_NAMEID!) {
  ecoverse(ID: $hubID) {
    opportunity(ID: $opportunityID) {
      displayName
      id
      nameID
      community {
        id
        displayName
      }
      context {
        id
        visuals {
          name
          id
        }
        ecosystemModel {
          id
          actorGroups {
            id
            name
            actors {
              name
            }
          }
        }
      }
    }
  }
}
