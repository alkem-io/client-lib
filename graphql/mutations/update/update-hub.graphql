mutation updateHub($hubData: UpdateHubInput!) {
  updateHub(hubData: $hubData) {
    nameID
    host {
      nameID
    }
    context {
      tagline
      visuals {
        id
        name
      }
    }
  }
}
