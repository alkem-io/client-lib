mutation updateSpace($spaceData: UpdateHubInput!) {
  updateHub(hubData: $spaceData) {
    nameID
    host {
      nameID
    }
    community {
      id
    }
    profile {
      tagline
      visuals {
        id
        name
      }
    }
  }
}
