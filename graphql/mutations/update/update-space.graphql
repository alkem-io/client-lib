mutation updateSpace($spaceData: UpdateSpaceInput!) {
  updateSpace(spaceData: $spaceData) {
    nameID
    host {
      nameID
    }
    community {
      id
    }
    profile {
      tagline
      visuals {
        id
        name
      }
    }
  }
}
