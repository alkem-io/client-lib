mutation uploadFileOnReference($file: Upload!, $uploadData: StorageBucketUploadFileInput!) {
  uploadFileOnReference(file: $file, uploadData: $uploadData) {
    id
    description
    name
    uri
  }
}